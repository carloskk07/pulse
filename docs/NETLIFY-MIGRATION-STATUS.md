# Netlify migration status

Initial migration target: Netlify project `reward-pulse`.

This bootstrap branch is operational only. Product source remains unchanged except for the portable `netlify.toml` build baseline. The GitHub Actions artifact step exists to produce a clean source archive in environments where direct Git clone is unavailable.

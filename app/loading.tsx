export default function Loading() {
  return (
    <main className="completion-page">
      <section className="completion-hero shell">
        <span className="section-kicker">Live state</span>
        <h1>Syncing your circuit.</h1>
        <p>PulseCircuit is loading the current account and reward state. No placeholder balance is invented while it loads.</p>
      </section>
    </main>
  );
}
